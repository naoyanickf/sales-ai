<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "営業太郎" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes compiled CSS from app/assets/builds/application.css %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <script defer src="https://unpkg.com/lucide@latest"></script>
  </head>

  <body class="bg-light min-vh-100 d-flex flex-column">
    <% unless user_signed_in? %>
      <header class="border-bottom bg-white">
        <div class="container d-flex flex-wrap align-items-center py-3 gap-3">
          <%= link_to "営業太郎", root_path, class: "navbar-brand fs-4 fw-semibold text-decoration-none text-dark" %>
          <div class="ms-auto d-flex gap-2">
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-primary" %>
            <%= link_to "無料登録", new_user_registration_path, class: "btn btn-primary" %>
          </div>
        </div>
      </header>
    <% end %>

    <% container_class = user_signed_in? ? "container-fluid px-0 h-100" : "container" %>
    <main class="flex-grow-1 <%= "py-4" unless user_signed_in? %>">
      <div class="<%= container_class %>">
        <% flash_wrapper_class = user_signed_in? ? "container-fluid" : "container" %>
        <%= render "shared/flash_messages", wrapper_class: flash_wrapper_class %>
        <%= yield %>
      </div>
    </main>

    <% unless user_signed_in? %>
      <footer class="bg-white border-top">
        <div class="container py-3 text-center text-body-secondary small">
          &copy; <%= Time.current.year %> 営業太郎. All rights reserved.
        </div>
      </footer>
    <% end %>

    <%= javascript_tag do %>
      function renderLucideIcons() {
        if (window.lucide && typeof window.lucide.createIcons === "function") {
          window.lucide.createIcons();
        }
      }

      if (document.readyState !== "loading") {
        renderLucideIcons();
      } else {
        document.addEventListener("DOMContentLoaded", renderLucideIcons);
      }
      document.addEventListener("turbo:load", renderLucideIcons);
      document.addEventListener("turbo:frame-load", renderLucideIcons);
      document.addEventListener("turbo:before-stream-render", function(event) {
        const originalRender = event.detail.render;
        event.detail.render = function() {
          originalRender.apply(this, arguments);
          requestAnimationFrame(renderLucideIcons);
        };
      });
    <% end %>
  </body>
</html>
