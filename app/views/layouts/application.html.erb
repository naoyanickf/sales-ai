<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sales Ai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes compiled CSS from app/assets/builds/application.css %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-light min-vh-100 d-flex flex-column">
    <header class="border-bottom bg-white">
      <div class="container d-flex flex-wrap align-items-center py-3 gap-3">
        <%= link_to "Sales AI", root_path, class: "navbar-brand fs-4 fw-semibold text-decoration-none text-dark" %>
        <div class="ms-auto d-flex gap-2">
          <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-primary" %>
          <%= link_to "無料登録", new_user_registration_path, class: "btn btn-primary" %>
        </div>
      </div>
    </header>

    <main class="flex-grow-1 py-4">
      <div class="container">
        <% flash.each do |type, message| %>
          <% bootstrap_class = case type.to_sym
             when :notice then "alert-success"
             when :alert then "alert-danger"
             else "alert-secondary"
             end %>
          <div class="alert <%= bootstrap_class %> mb-4" role="alert">
            <%= message %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>

    <footer class="bg-white border-top">
      <div class="container py-3 text-center text-body-secondary small">
        &copy; <%= Time.current.year %> Sales AI. All rights reserved.
      </div>
    </footer>
  </body>
</html>
