<section class="py-5">
  <div class="container">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
      <div>
        <p class="text-uppercase text-muted small mb-1">Chat History</p>
        <h1 class="h3 mb-0">チャット履歴</h1>
      </div>
    </div>

    <div class="card shadow-sm">
      <% if @chats.any? %>
        <div class="list-group list-group-flush">
          <% @chats.each do |chat| %>
            <%= link_to chat_path(chat),
                        class: "list-group-item list-group-item-action py-3" do %>
              <div class="d-flex justify-content-between align-items-start gap-3">
                <div>
                  <div class="fw-semibold mb-1"><%= chat.display_title %></div>
                  <div class="small text-muted">
                    <%= chat.product&.name || "製品未設定" %>
                    <% if chat.sales_expert&.name.present? %>
                      ・<%= chat.sales_expert.name %>
                    <% end %>
                  </div>
                </div>
                <div class="text-muted small text-nowrap">
                  更新: <%= l(chat.updated_at, format: :short) %>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="card-body text-center text-muted py-5">
          <p class="mb-2 fw-semibold">チャット履歴がありません。</p>
          <p class="mb-0">マイページトップから質問を送信してチャットを開始してください。</p>
        </div>
      <% end %>
    </div>
  </div>
</section>
