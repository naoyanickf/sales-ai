<% memberships = Array(@workspace_memberships) %>

<section class="mypage-layout">
  <div class="mypage-layout__wrapper d-flex flex-column flex-lg-row min-vh-100">
    <aside class="mypage-layout__sidebar bg-dark text-white p-4 d-flex flex-column">
      <div class="d-flex align-items-center gap-3 mb-4">
        <%= link_to root_path, class: "rounded-circle bg-white bg-opacity-10 d-flex align-items-center justify-content-center text-decoration-none text-white", style: "width: 48px; height: 48px;" do %>
          <span class="visually-hidden">ホーム</span>
          <i data-lucide="sparkles" aria-hidden="true"></i>
        <% end %>
        <div class="flex-grow-1">
          <% if memberships.any? %>
            <div class="sidebar-workspace-list rounded-3 w-100">
              <% memberships.each do |membership| %>
                <% workspace = membership.workspace %>
                <% next if workspace.nil? %>
                <div class="sidebar-workspace-list__item d-flex justify-content-between align-items-center gap-2">
                  <span class="text-truncate"><%= workspace.name %></span>
                  <%= link_to workspace_path(workspace), class: "btn btn-link text-white p-0", title: "#{workspace.name}を管理" do %>
                    <span class="visually-hidden"><%= workspace.name %>の管理ページ</span>
                    <i data-lucide="settings-2" aria-hidden="true"></i>
                  <% end %>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="text-white-50 small">
              まだワークスペースに参加していません。
            </div>
          <% end %>
        </div>
      </div>

      <div class="mt-auto pt-4 border-top border-white border-opacity-25">
        <div class="d-flex align-items-center justify-content-between gap-3">
          <div class="d-flex align-items-center gap-3 flex-grow-1" style="min-width: 0;">
            <div class="rounded-circle bg-white bg-opacity-10 d-flex align-items-center justify-content-center text-white" style="width: 44px; height: 44px;">
              <i data-lucide="user-round" aria-hidden="true"></i>
            </div>
            <div class="text-start flex-grow-1" style="min-width: 0;">
              <p class="mb-0 fw-semibold text-truncate" style="max-width: 160px;"><%= current_user.name %></p>
              <p class="mb-0 text-white-50 small text-truncate" style="max-width: 160px;"><%= current_user.email.split("@").first %></p>
            </div>
          </div>
          <%= link_to edit_profile_path, class: "btn btn-link text-white p-0 d-flex align-items-center justify-content-center", style: "width: 36px; height: 36px;", title: "設定" do %>
            <span class="visually-hidden">設定</span>
            <i data-lucide="settings" aria-hidden="true"></i>
          <% end %>
        </div>
        <div class="mt-3">
          <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-light w-100 text-start", form: { class: "w-100" } %>
        </div>
      </div>
    </aside>

    <div class="mypage-layout__body bg-light d-flex flex-column flex-grow-1">
      <div class="p-4 p-lg-5 flex-grow-1 d-flex flex-column">
          <header class="mb-5">
            <p class="text-uppercase text-primary fw-semibold small mb-2">Workspace Hub</p>
            <div class="d-flex flex-wrap justify-content-between align-items-center gap-3">
              <div>
                <h1 class="display-6 fw-bold mb-2">最新のワークスペースにアクセス</h1>
                <p class="text-body-secondary mb-0">参加中のワークスペースやアクティビティをここから素早く切り替えられます。</p>
              </div>
              <%= link_to "新規作成", new_workspace_path, class: "btn btn-primary btn-lg" %>
            </div>
          </header>

        <div class="row gy-4">
            <div class="col-12 col-xl-6">
              <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                  <h2 class="fs-5 fw-semibold mb-3">クイックスタート</h2>
                  <p class="text-body-secondary small mb-4">よく使うアクションをまとめました。</p>
                  <div class="d-grid gap-3">
                    <%= link_to "プロフィールを更新", edit_profile_path, class: "btn btn-outline-secondary text-start" %>
                    <%= link_to "新しいメンバーを招待", new_workspace_path, class: "btn btn-outline-secondary text-start" %>
                    <%= link_to "サポートに連絡", "#", class: "btn btn-outline-secondary text-start disabled", aria: { disabled: true } %>
                  </div>
                </div>
              </div>
            </div>

            <div class="col-12 col-xl-6">
              <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                  <h2 class="fs-5 fw-semibold mb-3">ヒント</h2>
                  <ul class="list-unstyled mb-0 text-body-secondary small">
                    <li class="mb-3">
                      <span class="fw-semibold text-dark d-block mb-1">新しい商談ノートを共有</span>
                      成功した商談のノートをワークスペースに投稿すると、チーム全体でナレッジを活用できます。
                    </li>
                    <li class="mb-3">
                      <span class="fw-semibold text-dark d-block mb-1">プロフィールを最新に保つ</span>
                      自己紹介や得意領域を設定しておくと、AIがより適切な提案を行うようになります。
                    </li>
                    <li>
                      <span class="fw-semibold text-dark d-block mb-1">定期的にチャンネルを整理</span>
                      不要になったワークスペースをアーカイブして、重要な情報を見つけやすくしましょう。
                    </li>
                  </ul>
                </div>
              </div>
            </div>
        </div>
      </div>
    </div>
    </div>
</section>
