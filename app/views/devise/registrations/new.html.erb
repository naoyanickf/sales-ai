<div class="container py-5">
  <div class="mx-auto" style="max-width: 480px;">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="fs-3 fw-semibold mb-3 text-center">無料登録</h1>
        <p class="text-body-secondary text-center mb-4">
          メールアドレスを登録してワークスペースを作成・参加できます。
        </p>

        <%= render "devise/shared/error_messages", resource: resource %>

        <%= form_with model: resource,
                      scope: resource_name,
                      url: registration_path(resource_name) do |form| %>
          <div class="mb-3">
            <%= form.label :email, "メールアドレス", class: "form-label" %>
            <%= form.email_field :email,
                                 autofocus: true,
                                 autocomplete: "email",
                                 class: "form-control" %>
          </div>

          <div class="mb-3">
            <%= form.label :password, "パスワード", class: "form-label" %>
            <% if @minimum_password_length %>
              <p class="text-body-secondary small mb-1">
                <%= @minimum_password_length %>文字以上で入力してください。
              </p>
            <% end %>
            <%= form.password_field :password,
                                    autocomplete: "new-password",
                                    class: "form-control" %>
          </div>

          <div class="mb-4">
            <%= form.label :password_confirmation, "パスワード（確認）", class: "form-label" %>
            <%= form.password_field :password_confirmation,
                                    autocomplete: "new-password",
                                    class: "form-control" %>
          </div>

          <div class="d-grid">
            <%= form.submit "登録する", class: "btn btn-primary btn-lg" %>
          </div>
        <% end %>

        <p class="mt-4 text-center small mb-0">
          すでにアカウントをお持ちの方は
          <%= link_to "こちら", new_session_path(resource_name), class: "link-primary" %>
        </p>
      </div>
    </div>
  </div>
</div>
